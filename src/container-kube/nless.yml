lang: 'Python'
homepage: https://github.com/mpryor/nothing-less
license: 'MIT'
desc:
  cn: 支持表格数据、分隔符推断与实时事件解析的 TUI 分页器
  en: 'A TUI pager with advanced support for tabular data, inferring/swapping delimiters, and real-time event parsing.'
x:
  source: https://github.com/mpryor/nothing-less
  eget: https://github.com/mpryor/nothing-less
rule:
  /pip:
    cmd: 'pip install nothing-less'
    reference: https://github.com/mpryor/nothing-less
  /eget:
    cmd: x eget mpryor/nothing-less
    reference: https://github.com/mpryor/nothing-less
binlist:
  - 'nless'

# complicated
footprint:
  behavior:
    runtime:
      type: tui
    network:
      local: true
      internet: true
    filesystem:
      read:
        desc:
          cn: 读取配置文件
          en: Reads config files
        sensitive: []
  resource:
    disk:
      size:
        install:
          estimate: 3.0 MB
          rating: 1
          desc:
            cn: 约 3.0 MB（估算）
            en: ~3.0 MB estimated
      paths:
        data: []
        config: []
    memory:
      idle:
        estimate: 10 MB
        desc:
          cn: 10 MB（估算）
          en: 10 MB estimated
    cpu:
      workload: ['compute']
      processes: 1
      peak:
        rating: 2
        desc:
          cn: 极低
          en: Negligible
