lang: Go
homepage: https://github.com/fluxcd/flux2
license: Apache-2.0
desc:
  en: Flux is a set of continuous and progressive delivery solutions for Kubernetes that are open and extensible
  cn: Flux 是一套连续和渐进的交付解决方案，是开放和可扩展的 Kubernetes
x:
  source: https://github.com/fluxcd/flux2
  eget: https://github.com/fluxcd/flux2
rule:
  /eget:
    cmd: x eget fluxcd/flux2
    reference: https://x-cmd.com/mod/eget
  darwin/brew:
    cmd: brew install fluxcd/tap/flux
    reference: https://fluxcd.io/flux/get-started/#install-the-flux-cli
binlist:
- flux

# complicated
footprint:
  behavior:
    runtime:
      type: cli
    network:
      local: true
      internet: true
    filesystem:
      read:
        desc:
          cn: 读取配置文件
          en: Reads config files
        sensitive: []
  resource:
    disk:
      size:
        install:
          estimate: 15 MB
          rating: 2
          desc:
            cn: 约 15 MB（估算）
            en: ~15 MB estimated
      paths:
        data: []
        config: []
    memory:
      idle:
        estimate: 25 MB
        desc:
          cn: 25 MB（估算）
          en: 25 MB estimated
    cpu:
      workload: ['compute']
      processes: 1
      peak:
        rating: 2
        desc:
          cn: 极低
          en: Negligible
