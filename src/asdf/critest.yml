lang: Go
homepage: https://github.com/kubernetes-sigs/cri-tools
license: Apache-2.0
desc:
  cn: Kubelet 容器运行时接口(CRI)的 CLI 和验证工具
  en: CLI and validation tools for Kubelet Container Runtime Interface (CRI)
x:
  source: https://github.com/kubernetes-sigs/cri-tools
  eget: https://github.com/kubernetes-sigs/cri-tools
rule:
  /eget:
    cmd: x eget kubernetes-sigs/cri-tools
    reference: https://x-cmd.com/mod/eget
  /wget:
    cmd: 'VERSION="v1.28.0" \

      && wget https://github.com/kubernetes-sigs/cri-tools/releases/download/$VERSION/critest-$VERSION-linux-amd64.tar.gz \

      && sudo tar zxvf critest-$VERSION-linux-amd64.tar.gz -C /usr/local/bin \

      && rm -f critest-$VERSION-linux-amd64.tar.gz

      '
    reference: https://github.com/kubernetes-sigs/cri-tools?tab=readme-ov-file#install-critest
binlist:
- critest

footprint:
  behavior:
    runtime:
      type: cli
    network:
      local: true
      internet: false
    filesystem:
      read:
        desc:
          cn: 读取配置文件
          en: Reads config files
        sensitive: []
  resource:
    disk:
      size:
        install:
          estimate: 20 MB
          rating: 1
          desc:
            cn: 安装 20 MB
            en: 20 MB install
      paths:
        data: []
        config: []
    memory:
      idle:
        estimate: 30 MB
        desc:
          cn: 30 MB
          en: 30 MB
    cpu:
      workload: ['io', 'compute']
      processes: 1
      peak:
        rating: 2
        desc:
          cn: 极低
          en: Negligible
