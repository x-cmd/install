lang: awk
homepage: https://github.com/deepin-community/dpkg-awk
desc:
  en: Gawk script to parse /var/lib/dpkg/{status,available} and Packages
  cn: 解析 /var/lib/dpkg/{status,available} 和 Packages 的 Gawk 脚本
x:
  source: https://github.com/deepin-community/dpkg-awk
rule:
  debian/apt:
    cmd: sudo apt-get install dpkg-awk
    reference: https://command-not-found.com/dpkg-awk
    dsnap: dpkg-awk
  ubuntu/apt:
    cmd: sudo apt-get install dpkg-awk
    reference: https://command-not-found.com/dpkg-awk
    dsnap: dpkg-awk
  kali/apt:
    cmd: sudo apt-get install dpkg-awk
    reference: https://command-not-found.com/dpkg-awk
    dsnap: dpkg-awk
  raspbian/apt:
    cmd: sudo apt-get install dpkg-awk
    reference: https://command-not-found.com/dpkg-awk
binlist:
- dpkg-awk
license: NOASSERTION

# complicated
footprint:
  behavior:
    runtime:
      type: library
    network:
      localhost: true
      lan: false
      internet: false
    filesystem:
      read:
        desc:
          cn: 读取配置文件
          en: Reads config files
  resource:
    disk:
      size:
        install:
          estimate: 6 KB
          rating: 1
          desc:
            cn: 约 6 KB（估算）
            en: ~6 KB estimated
      paths:
        data: []
        config: []
    memory:
      idle:
        estimate: 5 MB
        desc:
          cn: 5 MB（估算）
          en: 5 MB estimated
    cpu:
      workload: ['compute']
      processes: 1
      peak:
        rating: 2
        desc:
          cn: 极低
          en: Negligible
