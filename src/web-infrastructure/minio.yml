lang: Go
homepage: https://github.com/minio/minio
license: AGPL-3.0
desc:
  cn: MinIO 是一个高性能、兼容 S3 的对象存储
  en: MinIO is a high-performance, S3 compatible object store
x:
  source: https://github.com/minio/minio
  brew: minio/stable/minio
  yay: minio
  pip: minio
  npm: --save
rule:
  darwin/brew:
    cmd: 'brew install minio/stable/minio && \

      MINIO_ROOT_USER=admin MINIO_ROOT_PASSWORD=password minio server /mnt/data --console-address ":9001"

      '
    reference: https://min.io/download?license=agpl&platform=macos
  /go:
    cmd: go get github.com/minio/minio-go/v7
    reference: https://min.io/download?license=agpl&platform=linux
    dsnap: github.com/minio/minio-go/v7
  /npm:
    cmd: npm install --save minio
    reference: https://min.io/download?license=agpl&platform=linux
    dsnap: minio
  /pip:
    cmd: pip install minio
    reference: https://min.io/download?license=agpl&platform=linux
    dsnap: minio
  /stack:
    cmd: stack install minio-hs
    reference: https://min.io/download?license=agpl&platform=linux
  /vcpkg:
    cmd: vcpkg install minio-cpp
    reference: https://min.io/download?license=agpl&platform=linux
  /cargo:
    cmd: cargo add minio
    reference: https://min.io/download?license=agpl&platform=linux
    dsnap: minio
  arch/yay:
    cmd: yay -S minio
    reference: https://aur.archlinux.org/packages/minio
binlist:
- minio

# complicated
footprint:
  behavior:
    runtime:
      type: daemon
    network:
      listen: localhost
      connect:
        localhost: false
        lan: false
        internet: false
    filesystem:
      read:
        desc:
          cn: 读取配置文件
          en: Reads config files
        sensitive: []
  resource:
    disk:
      size:
        install:
          estimate: 21 MB
          rating: 2
          desc:
            cn: 约 21 MB（估算）
            en: ~21 MB estimated
      paths:
        data: []
        config: []
    memory:
      idle:
        estimate: 10 MB
        desc:
          cn: 10 MB（估算）
          en: 10 MB estimated
    cpu:
      workload: ['compute']
      processes: 1
      peak:
        rating: 2
        desc:
          cn: 极低
          en: Negligible
