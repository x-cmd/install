lang: Go
homepage: https://github.com/gitleaks/gitleaks
license: MIT
desc:
  cn: gitleaks 是一个 SAST 工具，用于检测和防止 git repos 中的硬编码秘密，如密码、api 密钥和令牌
  en: gitleaks is a SAST tool for detecting and preventing hardcoded secrets like passwords, api keys, and tokens in git repos
x:
  pkg:
    exist: true
  source: https://github.com/gitleaks/gitleaks
  eget: https://github.com/gitleaks/gitleaks
rule:
  /x:
    cmd: x pkg use gitleaks
    reference: https://x-cmd.com/install/
  /eget:
    cmd: x eget gitleaks/gitleaks
    reference: https://x-cmd.com/mod/eget
  /asdf:
    cmd: x asdf use gitleaks
    reference: https://github.com/jmcvetta/asdf-gitleaks
  darwin/brew:
    cmd: brew install gitleaks
    reference: https://formulae.brew.sh/formula/gitleaks#default
binlist:
- gitleaks

footprint:
  behavior:
    runtime:
      type: cli
    network:
      local: true
      internet: false
    filesystem:
      read:
        desc:
          cn: 读取配置文件
          en: Reads config files
        sensitive: []
  resource:
    disk:
      size:
        install:
          estimate: 20 MB
          rating: 1
          desc:
            cn: 安装 20 MB
            en: 20 MB install
      paths:
        data: []
        config: []
    memory:
      idle:
        estimate: 30 MB
        desc:
          cn: 30 MB
          en: 30 MB
    cpu:
      workload: ['io', 'compute']
      processes: 1
      peak:
        rating: 2
        desc:
          cn: 极低
          en: Negligible
